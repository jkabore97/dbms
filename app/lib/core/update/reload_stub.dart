/// Reloading the page is a browser thing; on a phone there is no page.
/// The banner never offers it there, so this is never reached — it exists
/// so the conditional import resolves on every platform.
void reloadPage() {}
